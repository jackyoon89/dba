# Matches : .*
# Matches : XC.*
# Matches : ((P|U)0.*|PROD*)


CONFIGURE BACKUP OPTIMIZATION ON;
CONFIGURE DEVICE TYPE DISK PARALLELISM 8 BACKUP TYPE TO BACKUPSET;
CONFIGURE CONTROLFILE AUTOBACKUP FORMAT FOR DEVICE TYPE DISK TO '/oracle/recovery_area/&1/autobackup/%F';
CONFIGURE ARCHIVELOG DELETION POLICY CLEAR;
CONFIGURE SNAPSHOT CONTROLFILE NAME TO '/oracle/recovery_area/&1/autobackup/snapcf_&1..f';
